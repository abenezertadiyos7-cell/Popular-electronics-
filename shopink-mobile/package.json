{
  "name": "shopink-mobile",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "test": "npx playwright test",
    "test:ui": "npx playwright test --ui",
    "build": "echo 'No build needed'",
    "analyze": "node bundle-size-analyzer/analyze.js",
    "ci": "npm test && npx @lhci/cli autorun"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@lhci/cli": "^0.12.0",
    "@playwright/test": "^1.42.0",
    "wait-on": "^7.0.1"
  }
}
